rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads and writes for data import
    // This will be replaced with the original rules after import is complete
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
