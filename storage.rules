rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Badge photos - allow authenticated users to read and write
    match /badges/{badgePhoto} {
      // Allow authenticated users to upload badge photos
      allow write: if request.auth != null
                   && request.resource.size < 5 * 1024 * 1024  // Max 5MB
                   && request.resource.contentType.matches('image/.*');

      // Allow authenticated users to read badge photos
      allow read: if request.auth != null;
    }

    // Applicant documents - allow authenticated users to read and write
    match /applicant-documents/{applicantId}/{document} {
      // Allow authenticated users to upload documents
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024;  // Max 10MB

      // Allow authenticated users to read documents
      allow read: if request.auth != null;
    }

    // Deny all other access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
